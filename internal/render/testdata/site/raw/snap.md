# Frozen snapshot 2026-07-07

A raw-layer file: frozen source material, never parsed or rendered as a page. It
exists so a citation link into raw/ has a real target on disk, even though the
site renders that link as a non-navigating frozen chip (R17).
