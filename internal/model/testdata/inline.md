# Inline fixture

A lede paragraph introducing the inline span kinds.

This paragraph carries **bold**, *italic*, ~~struck~~, and `code`, an
[external label with `code` inside](https://example.com) link whose text mixes
plain and code runs, plus a bare [VERIFY] badge and a [VERIFY: item name] badge
with a note.
