# Fences fixture

A lede paragraph introducing the fenced-block cases: a diagram, a tagged code
fence, and a bare fence.

```diagram
api/ source ──▶ build ──▶ image
```

```go
func main() {
	println("hello")
}
```

```
a bare fence with no language tag
```
